# recon-fast



Open Burp Suite: Ensure your Proxy Listener is active on 127.0.0.1:8080.

​Turn Intercept OFF: Unless you want to manually click "Forward" thousands of times.

​Run the script with -p:

​For a single target:

./recon.sh -d example.com -p

​For a list:
./recon.sh -l targets.txt -p
